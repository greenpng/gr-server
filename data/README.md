# Development datasets

This directory contains only safe reproducible datasets needed by local builds
and labs. Runtime databases, administrator state and OAuth state are generated
under ignored test data directories instead.
