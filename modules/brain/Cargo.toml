[package]
name = "gr-module-brain"
version.workspace = true
edition.workspace = true
build = "build.rs"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
gr-abi = { workspace = true }
serde_json = { workspace = true }
serde = { workspace = true }

[features]
default = []
plugin = []
